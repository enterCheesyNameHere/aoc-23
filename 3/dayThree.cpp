#include <iostream>
#include <fstream>
#include <string>

using namespace std;

const string PATH = getenv("SOURCEDIR");

int main() {
    ifstream FInput(PATH + "/input.txt");
    string line;

    return 0;
}